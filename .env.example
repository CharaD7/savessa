# Database
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSL=require

# Firebase (fill with your Firebase project settings)
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=

# App
APP_NAME=Savessa
APP_ENV=development

# SMTP for email OTP (optional for local dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
EMAIL_FROM_ADDRESS=no-reply@example.com
EMAIL_FROM_NAME=Savessa

# Application
APP_ENV=development

# Database
# Example connection details for Postgres. Copy to .env and replace with your own.
DB_HOST=some-host.aivencloud.com
DB_PORT=24322
DB_NAME=dbname
DB_USER=username
DB_PASSWORD=changeme
DB_SSL=require

# Full connection URI (common for CLIs/tools)
DATABASE_URL=postgres://username:changeme@some-host.aivencloud.com:24322/dbname?sslmode=require

